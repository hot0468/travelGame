# 여행설계사 (부산 여행 일정 설계 게임)

빌드·의존성 없는 단일 HTML 브라우저 게임. `index.html` 더블클릭으로 실행되며,
**file:// 에서 항상 동작해야 한다** (웹폰트·CDN·fetch·type=module 금지, classic script만).

## 구조

```
index.html      UI + 엔진 전부 인라인 (~1100줄). CSS → HTML → JS(엔진 → 상태 → 렌더 → 이벤트 → 자체점검)
data/reviews.js 장소 리뷰 3개씩(175곳 525개). tools/mkreview.js 가 생성한다(수기 편집 금지).
data/seoul.js   서울 — 뼈대만 있는 준비 중 지역. 새 지역을 만들 때 이 파일을 보고 시작한다.
data/busan.js   부산 데이터: REGIONS.busan = { mapImage, origins, starts, staminaTypes,
                sightTypes/foodTypes/ageTypes, pois(136), quests(6) }
data/subway.js  부산 도시철도 1~4호선(역 108개). REGIONS.busan.subway = { lines[], pos{} }.
                출처 공공데이터포털 부산교통공사_도시철도역사정보(이용허락 제한 없음).
data/transit.js 버스+지하철 통합 경로표(9,591쌍). REGIONS.busan.transit = { st[], ln[], ord[],
                lines{}, pair[] }. tools/mktransit.js 가 생성한다(수기 편집 금지).
assets/         지도 SVG. tools/mkmap.js 가 통계청 경계로 생성한다(수기 편집 금지).
assets/photos/  장소 사진 <POI id>.jpg + 유형별 배너 <POI id>.svg.
                사진이 우선하고 없으면 배너로 갈아탄다. 136곳 모두 채워져 있다.
admin.html      사진 크롭·저장 어드민. 팝업과 같은 236:104 로 잘라 assets/photos 에 넣는다.
tools/          검증·캡처 스크립트 (아래)
```

핵심 엔진 함수(index.html): `compute(state)` 일정 시뮬레이션(비용·시간·체력·위반),
`travel(a,b,mode,people,depart,weekend)` 이동시간(직선거리×1.35, 출퇴근 1.5배),
`drain()` 체력, `dowOf()` 요일. 상태는 전역 `S`, 렌더는 `render()` 하나로 전부 다시 그림.

## 지역 추가

첫 실행에 지역 선택창이 뜬다(고른 지역은 localStorage 에 남아 다시 묻지 않는다).
상단 지역 칩을 누르면 언제든 바꿀 수 있고, 바꾸면 짜던 일정은 비워진다 —
장소·의뢰가 지역마다 통째로 다르기 때문이다.

선택창은 `REGIONS` 를 훑어 만들어지므로 **지역을 더해도 UI 코드는 손대지 않는다.**
`pois`·`quests` 가 비어 있으면 "준비 중" 으로 뜨고 고를 수 없다.

1. `data/<지역>.js` 에 `REGIONS.<지역>` 을 등록한다(`data/seoul.js` 가 뼈대다).
   필수: `name, sightTypes, foodTypes, ageTypes, staminaTypes, dayNames, origins, starts, pois, quests`
2. `index.html` 에 `<script src="data/<지역>.js"></script>` 한 줄.
3. 지도는 `tools/mkmap.js` 를 그 지역 경계로 고쳐 만든다(`mapImage.bounds` 와 캔버스를 맞춘다).
4. 대중교통 실측이 있으면 `tools/mktransit.js` 로 경로표를 굽는다.
   없으면 `travel()` 이 직선거리 근사로 돌아간다(`real` 이 false 가 된다) — 그대로도 돌아간다.
5. 사진은 `tools/mkbanner.js` 로 유형별 배너부터 깔고, 실사는 `admin.html` 로 넣는다.
6. `node tools/balance.js` 로 그 지역 의뢰의 `par` 를 잡는다.

**해외도 같은 방식이다.** 통화·도시간 교통편만 그 나라에 맞게 쓰면 된다. 지도는 해당국
공공데이터나 Natural Earth(퍼블릭 도메인)를 쓰고, 대중교통은 GTFS 가 공개된 도시라면
`mktransit.js` 를 고쳐 실측을 넣을 수 있다.

## 검증 — 코드·데이터를 바꿨으면 반드시

```bash
node tools/all.js          # 스위트 14종 전부 (selftest/stam/buff/sub/dow/ret/day/rule/subway/transit/lug/pair/balance)
```

- **밸런스 재조정 절차**: POI·의뢰·엔진 수치를 바꾸면 `node tools/balance.js` 로 의뢰별
  탐색 상한을 재측정하고, `data/busan.js` 의 `par` 를 **상한의 62%**(`balance.js` 의 `PAR`)로
  맞춘다. 조정 후 all.js 재실행.
  - 별 기준은 `par×1.25`(★3)/`par`(★2)/`par×0.75`(★1)라 **par 를 상한에서 역산하는 순간
    `상한 ≥ par×1.25` 검사는 항상 참이 된다** — 동어반복이라 아무것도 못 걸러낸다.
    그래서 balance 는 **★3 선이 상한의 몇 %인가**(`CEIL`=85% 이하)로 판정한다.
    62% → ★3 선이 상한의 77.5%. 이전 78% 설정은 97.5%여서 사람 손에 닿지 않았다.
- balance 는 **시드 고정 + 반복수 고정(ITER=3000, 약 80초)**. 둘 다 상한값을 좌우하므로
  par 를 보정한 반복수를 바꾸면 보정이 무효가 된다. 빠른 확인만 원하면 `ITER=300 node tools/balance.js`.
  수단은 구간별 최적화 패스로 고른다 — 무작위로 굴리면 도보·지하철이 섞여 상한이 과소평가된다.
- 브라우저 자체점검: `index.html?test` 로 열면 콘솔에 동일 검사가 돈다.

## 장소 사진

팝업 상단 사진은 `assets/photos/<POI id>.jpg` 다. **없으면 그 자리가 아예 안 생긴다** —
136곳을 다 채울 필요 없이 있는 것만 보인다.

- **넣는 법**: `admin.html` 을 열어 장소를 고르고 사진을 끌어다 놓는다. 드래그·휠로 구도를
  잡고 저장하면 팝업과 같은 236:104 비율로 잘려 들어간다(기본 폭 600px).
  - 폴더에 바로 쓰려면 **로컬 서버로 열어야 한다**(`npx serve` 후 localhost 접속).
    File System Access API 가 `file://` 에서 차단되기 때문이다. 그냥 열면 저장이
    내려받기로 바뀌며, 받은 파일을 `assets/photos` 로 옮기면 결과는 같다.
- **자동 수집**: `node tools/mkphoto.js`(TourAPI, `.apikey` 에 `TOUR_API_KEY=...`) 와
  `node tools/mkcommons.js`(위키미디어 커먼즈). TourAPI 에는 **부산 주요 명소가 없어**
  (해운대·용궁사·오륙도·범어사) 커먼즈로 메운다. 받은 뒤 `node tools/resize.js` 로 줄이고,
  커먼즈 사진은 `node tools/mkcredits.js` 를 **꼭** 돌려야 저작권 표기가 붙는다.
- **배너**: `node tools/mkbanner.js` — 사진 없는 곳에 유형별 그림(.svg)을 그린다.
  식당·숙소는 공공사진이 없고 일부는 가상 시설이라 실사를 구할 수 없다.
- **라이선스**: 배포물에 들어가므로 직접 찍었거나 상업 이용이 허용된 것만 넣는다.
  출처 표시가 필요한 사진을 쓰면 앱에도 표기를 남겨야 한다.

## 화면 확인 (헤드리스 Edge)

```bash
node tools/mkcal.js -e "S.quest=REGIONS.busan.quests[0];S.xp=600;S.arriveId='ktx';render();" [--freeze]
powershell -NoProfile -File tools/shot.ps1 [-Out _shot.png -W 1600 -H 1000 -Scale 2]   # 스크린샷
powershell -NoProfile -File tools/probe.ps1                                            # DOM 값 읽기
```

- 프로브 패턴: 주입 코드에서 `document.title='PROBE '+JSON.stringify({...})` 로 값을 내보낸다.
- **헤드리스 함정**: `--virtual-time-budget` 아래에서는 CSS 애니메이션·smooth 스크롤이
  진행되지 않고 0% 프레임에 멈춘다. 등장 애니메이션 붙은 요소(dialog 등)가 안 보이면
  버그가 아니라 이것 — 캡처용으로만 `--freeze` 를 쓴다. `getBoundingClientRect` 는
  transform 중간값을 반환하므로 위치 검증은 `style.left`/`offsetWidth` 같은 레이아웃 값으로.
- 확인 끝나면 `_cal.html`, `_shot.png` 삭제 (gitignore 되어 있음).

## 규칙

- **이모지 금지.** 아이콘은 index.html 상단 Lucide 스프라이트(50여 종)를 `ic('이름')` 헬퍼로.
  새 아이콘: `curl -sfSL https://unpkg.com/lucide-static@latest/icons/이름.svg` 받아서
  `<symbol id="i-이름">` 으로 스프라이트에 추가.
- **큰 파일 수정**: index.html 은 정확한 문자열 치환으로. node 스크립트에서
  치환 수가 **정확히 1인지** 확인하고(0이면 미적용, 2 이상이면 엉뚱한 곳까지 바뀜),
  수정 후 항상 `new Function(스크립트부)` 문법 체크. CRLF 경고는 무해.
  - **먼 앵커까지 `slice` 금지.** `s.slice(s.indexOf(A), s.indexOf(B))` 로 함수 하나를 떼려다
    사이의 350줄을 통째로 날린 적이 있다. 바꿀 문자열만 좁게 잡아 치환한다.
  - `git checkout` 으로 복원하면 CRLF 로 돌아와 `\n` 기준 치환이 전부 실패한다. LF 로 정규화 후 진행.
- **`.ps1` 은 UTF-8 BOM 으로 저장.** Windows PowerShell 5.1 은 BOM 이 없으면 한글을 ANSI 로
  오독해 문자열 종결자를 잃고 파싱이 깨진다(`TerminatorExpectedAtEndOfString`).
  `[IO.File]::WriteAllText($p, $txt, [Text.UTF8Encoding]::new($true))`.
- **주석·커밋 메시지는 한국어.**
- **git 커밋·푸시는 사용자가 요청할 때만.** 임의로 올리지 않는다.
- 임시 스크립트는 작업 후 삭제.

## 라이선스 (배포물이므로 지켜야 함)

- 지도 `assets/busan-map-kostat.svg`: 통계청 2018 센서스용 행정구역경계에서 부산 16개
  구·군만 뽑아 `tools/mkmap.js` 가 그린다. 원본이 "Free to share or remix" 라 **파생물에
  라이선스가 따라붙지 않는다** — 앱 하단·README 의 출처 표시만 지키면 상업적 이용도 자유다.
  (이전에는 위키미디어 CC BY-SA 지도였다. SA 전염과 저작자 표기 의무를 없애려고 바꿨다.)
  - 재생성: `node tools/mkmap.js`. 캔버스·bounds 를 `data/busan.js` 의 mapImage 와 같게 맞추므로
    **POI 좌표는 건드리지 않아도 된다**(교체 후 136곳 전부 제 구역 안에 들어옴을 확인했다).
  - 원본 저장소(southkorea/southkorea-maps)의 `gadm/` 계열은 **비상업 전용**이다. 반드시
    `kostat/` 경로만 쓴다.
- 지도 `assets/fukuoka-map.svg`: 국토교통성 **국토수치정보**(행정구역 N03)를 가공해
  `tools/mkmap-fukuoka.js` 가 그린다. 정부표준이용약관 2.0 — 상업 이용·가공·재배포 자유,
  **CC BY 4.0 과 호환**이라 SA 전염이 없다. 출처 표시는 의무다(앱 하단).
  - 후쿠오카현 시구정촌이 72개라 코드를 손으로 고르지 않는다 — `bounds` 에 걸치는 것만
    자동으로 담는다(24곳). 원본 해안선이 촘촘해 Douglas-Peucker 로 솎아낸다(980KB → 103KB).
  - 중계 저장소: github.com/niiyz/JapanCityGeoJson (원본을 시구정촌별로 쪼개 둔 것).
- 후쿠오카 지하철 `data/fukuoka-subway.js`: kuwayamamasayuki/GTFS-FukuokaCitySubway(MIT)
  의 GTFS 를 `tools/mksubway-fukuoka.js` 가 가공한다. **공식 배포가 아니라 개인이 만든
  비공식 데이터**라 다이어 개정을 바로 못 따라갈 수 있다. MIT 라 이용 제약은 없다.
  - 일본 GTFS 저장소(api.gtfs-data.jp)에는 후쿠오카시 지하철·니시테츠 버스가 **없다**.
    후쿠오카현 피드 40개는 전부 주변 시·정촌 커뮤니티버스라 게임 장소와 안 맞는다.
  - 후쿠오카 **버스는 여전히 근사**지만, 장소가 모두 역에서 1.5km 안이라 실제로 근사가
    쓰이는 구간이 거의 없다.
- 니시테츠 `data/fukuoka-nishitetsu.js`: **손으로 적은 데이터**다(생성기 없음).
  니시테츠는 GTFS 를 공개하지 않아 공식 시각표·노선도를 보고 넣었다. 다자이후가 의뢰 f2 의
  필수 코스인데 지하철역에서 11.6km 라 그냥 두면 98분이 나왔다 — 넣은 뒤 55분이다(실제 40~45분).
  - 천신오무타선은 역이 40개가 넘지만 게임이 쓰는 텐진~후츠카이치 구간의 급행 정차역만 담는다.
  - 급행은 중간역을 통과하므로 subwayPath 가 더하는 `DWELL`(25초)만큼 빼서 적었다.
  - 니시테츠 텐진역과 지하철 텐진역을 같은 "텐진" 으로 두어 환승이 자동으로 잡힌다.
- Lucide 아이콘: ISC. 스프라이트 상단 주석 유지.

## 게임 규칙 요약 (수치 바꿀 때 참고)

- 이동: 도보4.5/버스18/지하철26/택시24/렌터카28 km/h. 출퇴근(07:30-09:30, 18:00-20:00) 버스·택시·
  렌터카 최대 1.5배, 도보·지하철 무관, 주말 무관. 교통패스 버프는 버스+지하철 일 6천원 상한.
  각 수단은 주행시간에 `walk`(정류장까지 도보)+`wait`(배차·주차) 를 더한다 — 버스5+3, 지하철4+2,
  택시0+2, 렌터카3+7분. `travel()` 이 `walkMin`/`waitMin` 로 내보내 UI 에 따로 표시한다.
  정체 배수는 **주행 구간에만** 붙는다(걷고 기다리는 시간은 길이 막혀도 그대로).
  버스·지하철 **소요시간**은 이제 실측이다 — `data/transit.js` 의 통합 경로표를 쓴다.
  표에 없는 지점(임의 좌표)만 직선×1.35 근사로 남는다. `travel()` 이 `real` 로 어느 쪽인지 알린다.
- **대중교통 통합 경로**: `data/transit.js` 가 버스+지하철을 한 그래프로 풀어 둔 표다
  (`transitPath(a,b)`). 정류장·역을 노드로 두고, 버스는 노선 순서대로 정류장 좌표를 누적한
  거리 ÷ 18km/h + 정차 20초, 지하철은 `subway.gap` 실측, 환승은 400m 안 도보 + 승차대기
  (버스 5분·지하철 3분)로 잇는다. **지하철만으로 못 가던 곳이 이걸로 풀린다** — 태종대(최근접역
  7km)·해동용궁사(동해선 미포함)·범어사(산 위) 등. 쌍의 55%가 버스+지하철 혼합 경로다.
  - **미리 굽는 이유**: 그래프가 노드 8,900·간선 18만이라 브라우저에서 한 쌍에 0.5초가 든다.
    `render()` 마다 전 구간을 다시 계산하는 구조라 그 값으로는 못 쓴다. 구워 두면 조회가
    쌍당 0.005ms 다. **장소를 추가·이동하면 `node tools/mktransit.js` 로 표를 다시 만들어야 한다.**
  - 표는 **상삼각만** 담는다(a→b 와 b→a 는 평균 1.9분 차이뿐). 역방향은 구간 순서와 양 끝을
    뒤집어 읽는다. 이름·노선은 사전 인덱스로 뺐다 — 그대로 쓰면 2MB, 지금은 617KB.
  - 재생성: `node tools/mktransit.js` (`.apikey` 에 `BUSAN_BIMS_KEY=...`. **Encoding 키**를
    넣는다 — 키를 쿼리에 그대로 붙이므로 Decoding 키는 403 이 난다. 응답은 임시폴더 캐시라
    재실행은 오프라인으로 된다).
  정류장은 반경 0.5km 안에서 고르고, 노선이 하나도 안 잡히면 `FAR`(1.6km)까지 넓힌다 —
  그렇게 잡힌 13곳(태종대 1.4km·가덕도 1.5km 등)은 UI 가 "걸어서 갈 거리가 아니다" 로 경고한다.
  마을버스는 API 노선 목록에 없어 일부 정류장이 노선 없이 버려진다(파크 하얏트 앞 정류장 등).
- **지하철 노선 안내**: `data/subway.js` 로 실제 1~4호선 경로·환승을 낸다(`subwayPath(a,b)`).
  양 끝의 최근접역을 잡아 역|노선 그래프에서 다익스트라를 돌린다. 가중치는 **실측 역간 소요시간**
  (`gap[노선][i] = [거리km, 소요초]`, 출처: 부산교통공사 역간 거리·소요시간 2025-03-25) + 정차
  `DWELL`(25초), 환승은 `TRANSFER_SEC`(300초). 역간 편차가 커서(부전~서면 65초 vs 증산~호포 250초)
  정거장 수로 세면 경로가 틀린다 — 부산역→해운대는 서면 1회 환승 45.5분 vs 연산·수영 2회 46.4분으로
  0.9분 차이라, 옛 `TRANSFER_COST=5` 손튜닝이 겨우 맞추던 판정을 이제 데이터가 결정한다.
  `travel()` 이 이 승차시간을 쓴다 — 단 **양쪽 최근접역이 2km 안일 때만**이고(POI 쌍의 55%),
  더 먼 곳은 그 구간을 실제로 버스·택시로 가므로 근사로 남긴다.
  최근접역이 3km 넘는 장소가 18곳이라(태종대 7.0km 등) 최근접역 도보를 그대로 `access` 로
  돌리면 안 된다 — 실제로는 그 구간을 버스·택시로 간다. UI 에 경고 문구로 처리했다.
- **`access`(POI, 분)**: 정류장·역에서 문 앞까지가 유별나게 먼 곳의 추가 도보. 대중교통에만
  양 끝 모두 붙고 택시·렌터카·도보에는 안 붙는다. 직선거리 모델이 볼 수 없는 지형 보정용 손잡이라
  **실측값이 있을 때만 넣는다.** 현재 감천문화마을 21분뿐(네이버 실측 버스 46 / 지하철 37 → 모델 44 / 38).
  다른 언덕·외곽 장소(흰여울·황령산·이기대 등)는 실측 전까지 0 이라 여전히 낙관적이다.
- 체력: 이동 소모(도보.45/버스.25/지하철.20/택시.10/렌터카.13 분당), 관광 .16, 식사 -.15(카페 절반).
  수면<8h → 다음날 한계 감소. 타입: 아침형(23시 넘기면 페널티)/야행성(새벽 1.6배)/
  에너자이저(×0.7)/멀미(이동 ×2).
- 요일: `startDow` 기준, `closed:[요일]` 휴무, 금·토 체크인 숙박 1.4배.
- 숙소 등급: `SLEEP_TIER` 로 다음날 체력 한계가 달라진다(게스트하우스 ×.94 / 모텔 ×1 / 호텔 ×1.08).
  숙박비는 `ceil(인원/2)` 객실이라 **4인 의뢰에선 최고급 호텔이 예산에 안 들어간다** — 최고급 5곳이
  쓰이는 곳은 2인·고예산인 q6 뿐이다. 비싼 숙소를 더 넣으려면 2인 의뢰를 함께 넣어야 한다.
- 귀가 마감: 의뢰별 `endBy`(자정 기준 분). 마지막 날 그 시각까지 출발지에 도착해야 제출 가능.
- 도시간 교통편: 귀가편을 지정하면 요금·소요시간을 **왕복으로** 계산한다(요금 ×2, 도착편 `min`
  만큼 이동시간 가산). 마감 판정은 출발지점 도착 기준이라 이 구간은 제외. 체력은 올 때만
  `origins[].drain` 으로 깎는다.
- **예매(1일차 시작 시각)**: 수단을 고르면 그 회사 예매 화면(`#book`)이 뜨고 거기서 편을 끊는다.
  기차는 코레일·비행기는 항공사 간판이 붙는다(`BRANDS`, `o.brand` 로 덮어쓸 수 있다).
  시간대는 `SLOTS` 4개(새벽·오전·오후·저녁)에 시간대마다 3편씩. **늦게 떠나는 편일수록 싸다**
  (새벽 ×1.25 / 오전 ×1.10 / 오후 ×0.95 / 저녁 ×0.80, 100원 단위 반올림) — 대신 늦게 닿은 만큼
  첫날이 짧다. 23시(`LAST_ARRIVE`) 넘겨 닿는 편은 팔지 않아 고속버스 저녁편 등이 빠진다.
  고른 편의 출발 시각이 `S.depMin` 이고 **1일차는 그 편의 도착 시각에 시작한다**
  (2일차부터는 예전대로 기상 시각 선택). `S.depMin` 이 없으면 `origins[].arrive`·`cost` 정가
  그대로라 도구 스크립트는 예매 없이도 돈다.
  - 하루 한두 편뿐이라 시간대에 안 맞는 노선(밤배·야간열차)은 데이터가 직접 적는다:
    `deps: [{ dep, arrive, fare, no }]`. `dep` 이 음수면 전날 출발(제주 밤배 -210 = 전날 20:30,
    카멜리아 밤배 -90 = 전날 22:30). 이때 시간대 배수는 안 붙고 `fare` 를 그대로 받는다.
- **자차 비용**: 표값이 없으니 `유류비 + 고속도로 통행료`다. 유류비 = `km ÷ fuel.kmpl × fuel.price`
  (지역의 `fuel`, 기본 12km/L·1,700원/L), 통행료는 `origins[].toll`. **0~5시 출발이면 통행료
  심야할인 50%** — 자차만 새벽 출발이 03:00까지 당겨진다(`SLOTS[].drive`). 경비 내역에는
  유류비·통행료가 따로 잡히고, 자차는 차 `ceil(인원/4)`대 기준이라 4인 의뢰에서 가장 싸다.
- **짐**: 의뢰가 정해서 온다(`q.luggage`). 도착부터 귀가까지 따라다니고, 숙소에 체크인하거나
  짐 보관소(`sub:'locker'`)에 맡기면 그때부터 빈 몸이다. 마지막 날 아침에는 체크아웃하며 다시 든다.
  - `LUGGAGE`: 가벼운 짐(×1) / 기내용 캐리어(×1.12) / 대형 캐리어(×1.3, **시내버스 불가**).
  - 배수는 수단에 따라 다르다(`LUG_MODE`) — 도보·지하철은 그대로, 버스 0.8, 택시·렌터카 0.15.
    트렁크에 싣고 끝나는 것과 끌고 계단을 오르내리는 것은 다르다.
  - **배수를 키우면 의뢰가 통째로 불가능해진다.** 처음 1.25/1.6 으로 넣었더니 q4(체력 75 ·
    멀미 2배 · 대형 캐리어)가 지하철 60분에 38점이 날아가 탐색 상한이 392→142 로 주저앉았다.
    다른 페널티와 곱해지는 값이라 1.4 를 넘기지 않는다(`lugtest.js` 가 지킨다).
  - 짐 보관소는 `type:'sight'` + `sub:'locker'` 다. 새 type 을 만들면 경비·정렬·통계가 전부
    갈라진다. **관광지 집계에는 안 들어간다** — joy 0 짜리로 손님 기대를 채울 수는 없다.
- **리뷰**: 팝업에 "다녀온 사람들" 3줄이 붙는다(`data/reviews.js`). **읽기 전용 힌트다** —
  점수·체력에 관여하지 않고, 이미 있는 규칙(짐·도보·연령 선호·휴무·영업시간·가격)을
  손님 말투로 드러낼 뿐이다. `revtest.js` 가 엔진이 REVIEWS 를 참조하지 않는지 검사한다.
  - `tools/mkreview.js` 가 장소 데이터를 보고 조건에 맞는 문장을 고른다 — 역에서 2km 넘으면
    "걷는 시간이 길어서…", 값이 중앙값의 2배면 "값은 좀 나가지만…" 식이다.
    **장소를 고치면 다시 구워야** 내용이 따라온다.
  - 항목마다 `no` 로 "이 종류에는 안 붙는다" 를 적는다. 코인락커에 "밤에 가면 분위기가",
    숙소에 "두어 시간은 잡아야" 가 붙던 것을 그렇게 걸렀다.
  - 시드를 고정해 매번 같은 문장이 나온다 — 안 그러면 diff 가 지저분하고 팝업을 여닫을
    때마다 말이 바뀐다.
- 선호: `ageTypes[연령].likes` 에 든 관광지 sub 는 만족도 +3.
- 점수 = 만족도×10 − 이동분 + 예산잔여%×100. 별: par×1.25↑=3, par↑=2, par×0.75↑=1.
